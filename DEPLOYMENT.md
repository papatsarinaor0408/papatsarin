# คู่มือ Deploy สำหรับฝ่าย IT

**ระบบพิจารณาแผนพัฒนาบุคลากร (โรงไฟฟ้าบางปะกง) ปี 2570**

เอกสารนี้จัดทำสำหรับทีม IT ที่จะนำเว็บไซต์นี้ขึ้น Web Server ภายในองค์กร

---

## 1. ประเภทของระบบ

ระบบนี้เป็น **Static Web Application** (ไฟล์ HTML/CSS/JavaScript ล้วนๆ) ไม่มีส่วนที่เป็น
Server-side code, ไม่ต้องใช้ Database, ไม่ต้องใช้ Runtime อย่าง Node.js/PHP/.NET ใดๆ ทั้งสิ้น
เพียงวางไฟล์บน Web Server ที่ให้บริการไฟล์ static (serve ไฟล์ตรงๆ) ก็ใช้งานได้ทันที

**Entry point:** `index.html`

การประมวลผลข้อมูล (นำเข้าไฟล์ Excel, กรอง, สรุปผล, บันทึกผลการพิจารณา) ทั้งหมดทำงาน
**ในเบราว์เซอร์ของผู้ใช้เท่านั้น** ไม่มีการส่งข้อมูลกลับไปที่ Web Server หรือที่ใดๆ ภายนอกเครื่อง

---

## 2. รายการไฟล์/โฟลเดอร์ที่ต้อง Deploy

```
(root ของเว็บไซต์ หรือ subfolder ก็ได้)
├── index.html                      ← หน้าเว็บหลัก (entry point)
├── README.md                       ← คู่มือสำหรับผู้ดูแลระบบ
├── DEPLOYMENT.md                   ← เอกสารนี้
└── assets/
    ├── css/
    │   └── style.css               ← สไตล์/ธีมของหน้าเว็บ
    └── js/
        ├── schema.js                ← นิยามคอลัมน์ Excel และสถานะการพิจารณา
        ├── sampleData.js            ← ข้อมูลตัวอย่าง (สมมติทั้งหมด ไม่ใช่ข้อมูลจริง)
        ├── importer.js              ← ตัวอ่านไฟล์ Excel/CSV
        ├── charts.js                ← เครื่องมือวาดกราฟ (SVG, ไม่พึ่งไลบรารีภายนอก)
        ├── app.js                   ← ตรรกะหลักของแอป (state, ตัวกรอง, การพิจารณา)
        └── vendor/
            ├── xlsx.full.min.js     ← ไลบรารี SheetJS (อ่านไฟล์ Excel) แนบมากับระบบ ไม่โหลดจาก CDN
            └── xlsx.LICENSE.txt     ← สัญญาอนุญาตของไลบรารีข้างต้น
```

ทุกไฟล์ข้างต้น **จำเป็นต่อการทำงาน** ห้ามตัดไฟล์ใดออก มิเช่นนั้นหน้าเว็บจะใช้งานไม่ได้
(เช่น ถ้าขาด `xlsx.full.min.js` จะนำเข้าไฟล์ Excel ไม่ได้, ถ้าขาดไฟล์ `.js` ตัวใดตัวหนึ่งใน
`assets/js/` หน้าเว็บจะไม่แสดงผลเลยเพราะสคริปต์โหลดไม่ครบ)

**ไม่มี** ไฟล์ข้อมูลจริงหรือข้อมูลส่วนบุคคลของพนักงานรวมอยู่ใน package นี้ —
`assets/js/sampleData.js` เป็นข้อมูลสมมติสำหรับสาธิตหน้าตาก่อนผู้ใช้นำเข้าไฟล์จริงเองเท่านั้น
ไฟล์ Excel ของจริงจะถูกเลือก/อ่านโดยผู้ใช้แต่ละคนในเบราว์เซอร์ของตัวเอง ไม่ได้อัปโหลดเข้า Server

---

## 3. Web Server Requirements

- Web server ที่ให้บริการไฟล์ static ได้ เช่น **IIS, Apache HTTPD, Nginx** (หรือแม้แต่
  static hosting/S3-compatible storage ก็ใช้ได้)
- **ไม่ต้องมี** application runtime (ไม่ต้อง Node.js, ไม่ต้อง PHP, ไม่ต้อง .NET)
- **ไม่ต้องมี** Database ใดๆ
- Deploy ที่ root domain (เช่น `https://intranet.example.local/`) หรือใน **subfolder**
  (เช่น `https://intranet.example.local/pdp2570/`) **ก็ได้ทั้งคู่** — path ของ CSS/JS/ไลบรารี
  ทั้งหมดในไฟล์ `index.html` เป็น relative path (ไม่มี `/` นำหน้า) จึงย้ายไปอยู่ folder ไหนก็ทำงานถูกต้อง
  (ได้ตรวจสอบและทดสอบ subfolder deployment แล้ว — ดูหัวข้อ 8)

---

## 4. Browser Requirements

รองรับเบราว์เซอร์ evergreen รุ่นใหม่ทั่วไป:

- **Google Chrome** (แนะนำ — ทดสอบแล้วในเอกสารนี้)
- **Microsoft Edge** (Chromium-based, Edge 90 ขึ้นไป) — ใช้ rendering/JS engine
  ตระกูลเดียวกับ Chrome จึงคาดว่าทำงานได้เหมือนกัน (ดูหมายเหตุการทดสอบในหัวข้อ 10)
- Firefox รุ่นใหม่ก็ใช้งานได้เช่นกัน (ไม่ได้ทดสอบอย่างเป็นทางการในรอบนี้)

โค้ดใช้เฉพาะ JavaScript/CSS มาตรฐาน (ES6+, SVG DOM, `localStorage`, `FileReader`) ไม่มีการเรียก
API เฉพาะของเบราว์เซอร์ใดเบราว์เซอร์หนึ่ง และ**ไม่รองรับ Internet Explorer**

---

## 5. MIME / Content-Type ที่จำเป็น

Web server ต้อง serve ไฟล์ด้วย content-type ที่ถูกต้องตามนามสกุลไฟล์ (ค่ามาตรฐานของ
Web server ส่วนใหญ่ตั้งไว้ให้อยู่แล้ว ไม่ต้อง config เพิ่มในกรณีทั่วไป):

| นามสกุลไฟล์ | Content-Type ที่ควรตั้ง |
|---|---|
| `.html` | `text/html; charset=utf-8` |
| `.css`  | `text/css` |
| `.js`   | `text/javascript` หรือ `application/javascript` |
| `.txt`  | `text/plain` |

**ข้อควรระวัง:** ต้องตั้งค่า `charset=utf-8` ให้ถูกต้อง (หรือปล่อยให้ browser ใช้ `<meta charset="UTF-8">`
ในไฟล์ `index.html` เอง) มิฉะนั้นข้อความภาษาไทยในหน้าเว็บอาจแสดงผลผิดเพี้ยน (mojibake)

---

## 6. HTTPS Recommendation

แนะนำให้ deploy ผ่าน **HTTPS** แม้จะเป็นเว็บไซต์ภายในองค์กร (intranet) เนื่องจาก:

- ฟอนต์ Kanit โหลดจาก Google Fonts ผ่าน HTTPS (`https://fonts.googleapis.com`) — หน้าเว็บที่ให้บริการผ่าน
  HTTPS จะไม่มีปัญหา mixed-content ใดๆ
- เป็นแนวปฏิบัติที่ดี (best practice) สำหรับเว็บไซต์ภายในที่เกี่ยวข้องกับกระบวนการพิจารณา/อนุมัติของผู้บริหาร
  แม้ตัวระบบจะไม่ส่งข้อมูลออกนอกเบราว์เซอร์ก็ตาม

ระบบไม่ได้เรียกใช้ API ใดๆ ที่บังคับต้องอยู่บน Secure Context (HTTPS) — จะ deploy ผ่าน HTTP ธรรมดา
ก็ยังใช้งานได้ปกติ แต่ HTTPS คือคำแนะนำเพื่อความปลอดภัยโดยรวม

---

## 7. Network / Font Dependency

- **Google Fonts (Kanit):** หน้าเว็บพยายามโหลดฟอนต์ Kanit จาก `fonts.googleapis.com` /
  `fonts.gstatic.com` เพื่อความสวยงามของตัวอักษรไทย **หากเครื่องผู้ใช้ไม่มีอินเทอร์เน็ต/ถูกไฟร์วอลล์กัน
  โดเมนดังกล่าว ระบบจะยังทำงานได้ปกติทุกฟังก์ชัน** เพียงแต่ตัวอักษรจะ fallback ไปใช้ฟอนต์ระบบแทน
  (`Noto Sans Thai` หรือ `Tahoma`) — ได้ทดสอบยืนยันกรณีนี้แล้ว (ตัดการเชื่อมต่อ CDN ฟอนต์)
  ไม่พบข้อผิดพลาดใดๆ ในหน้าเว็บ
- **ไลบรารีอ่าน Excel (SheetJS):** แนบไว้ในเครื่อง (`assets/js/vendor/xlsx.full.min.js`) แล้ว
  **ไม่ได้โหลดจาก CDN ภายนอก** จึงใช้งานได้แม้เครือข่ายภายในไม่มีอินเทอร์เน็ตออกนอกองค์กรเลย
- ไม่มี dependency อื่นใดที่เรียกออกนอกเครื่อง/นอกโดเมนอีก

**สรุป:** ถ้าต้องการให้ระบบทำงานได้ 100% แบบ offline/intranet-only ให้อนุญาต (หรือปิดกั้นก็ได้)
โดเมน Google Fonts ตามความเหมาะสมของนโยบายเครือข่ายองค์กร — ไม่กระทบฟังก์ชันการทำงานหลัก
กระทบเฉพาะรูปลักษณ์ตัวอักษรเท่านั้น

---

## 8. วิธีทดสอบหลัง Deploy

หลังวางไฟล์บน Web Server แล้ว แนะนำให้ทดสอบตามลำดับนี้:

1. เปิด URL ของเว็บไซต์ (เช่น `https://intranet.example.local/pdp2570/`) ด้วยเบราว์เซอร์
   → ต้องเห็นหัวข้อ **"ระบบพิจารณาแผนพัฒนาบุคลากร (โรงไฟฟ้าบางปะกง) ปี 2570"** และแดชบอร์ดข้อมูลตัวอย่างแสดงผลทันที
2. เปิด Developer Tools (กด `F12`) → แท็บ **Console** → ต้อง**ไม่มี error สีแดง**
   (คำเตือน/error เกี่ยวกับการโหลดฟอนต์ Google Fonts เพียงอย่างเดียวถือว่าไม่กระทบการทำงาน หากเครือข่ายภายในบล็อกโดเมนนั้นไว้)
3. กด **"นำเข้าข้อมูล"** มุมขวาบน แล้วเลือกไฟล์ Excel (`.xlsx`) ตัวอย่างของหน่วยงาน
   → ต้องเห็นจำนวนแผนที่นำเข้าได้ถูกต้อง และแดชบอร์ด/กราฟอัปเดตตามข้อมูลใหม่
4. ทดสอบตัวกรอง (หน่วยงาน/ประเภทหลักสูตร/ปัจจัยนำเข้า/สถานะ/คำค้นหา) ที่แถบตัวกรอง
   → กราฟและตารางต้องกรองผลตามที่เลือกถูกต้อง
5. ไปที่แท็บ **"พิจารณาแผนรายฉบับ"** เปิดดูรายละเอียดแผนหนึ่งรายการ แล้วทดสอบปุ่มตัดสินใจครบทั้ง 3 สถานะ
   (**เห็นชอบ / เห็นชอบแต่ให้ทบทวน / ไม่เห็นชอบ**) รวมถึงปุ่ม **"รอพิจารณา"** เพื่อย้อนกลับสถานะ
   → ต้องบันทึกสถานะและหมายเหตุได้ถูกต้อง (หมายเหตุจำเป็นเฉพาะกรณี "ให้ทบทวน"/"ไม่เห็นชอบ")
6. รีเฟรชหน้าเว็บ (`F5`) → ผลการพิจารณาที่บันทึกไว้ในขั้นตอนที่ 5 ต้องยังคงอยู่
   (ยืนยันว่า local persistence ทำงานถูกต้อง)
7. ทดสอบแท็บ **"สรุปตามหน่วยงาน"** → ตารางสรุปต้องแสดงผลถูกต้องตามข้อมูลที่นำเข้า
8. ทำซ้ำขั้นตอน 1–3 บน **เบราว์เซอร์อีกตัว** (เช่น ทดสอบทั้ง Chrome และ Edge) เพื่อยืนยันว่าแสดงผล
   และทำงานเหมือนกัน

หากทุกข้อผ่าน ถือว่า deploy สำเร็จและพร้อมใช้งาน

---

## 9. ผลการทดสอบที่ทำไว้แล้วก่อนส่งมอบ package นี้ (โดยผู้พัฒนา)

- ทดสอบ deploy ในโครงสร้าง subfolder จำลอง (ไม่ใช่ root) แล้วเปิดผ่าน local web server →
  โหลด CSS/JS/ไลบรารีครบถ้วน ไม่มี path ผิดพลาด
- นำเข้าไฟล์ Excel จริงของหน่วยงาน (โครงสร้างคอลัมน์เดียวกับที่ใช้งานจริง) → นำเข้าสำเร็จ
  ครบทุกแถว ไม่มี error
- ทดสอบ Dashboard, ตัวกรองทุกมิติ, กราฟทุกประเภท (โดนัท/แท่งซ้อน/งบประมาณ), Review Workflow
  ครบทั้ง 3 สถานะ + ปุ่มย้อนกลับเป็น "รอพิจารณา" → ทำงานถูกต้องครบถ้วน
- ทดสอบ local persistence (รีเฟรชหน้าเว็บแล้วผลการพิจารณายังอยู่) → ผ่าน
- ตรวจ Browser Console ขณะทดสอบทั้งหมด → ไม่มี JavaScript error (มีเฉพาะคำเตือนโหลดฟอนต์ Google Fonts
  ล้มเหลวในสภาพแวดล้อมทดสอบที่ไม่มีอินเทอร์เน็ต ซึ่งไม่กระทบการทำงาน)
- ทดสอบบน **Google Chrome (Chromium engine)** ครบทุกข้อข้างต้น
- **Microsoft Edge**: ไม่สามารถติดตั้ง/รันทดสอบอัตโนมัติได้ในสภาพแวดล้อมพัฒนาที่ใช้จัดทำ package นี้
  (ไม่มีตัวติดตั้ง Edge ในเครื่องทดสอบ) — เนื่องจาก Edge รุ่นปัจจุบันใช้ Chromium engine เดียวกับ Chrome
  และโค้ดไม่มีการเรียก API เฉพาะเบราว์เซอร์ใดๆ จึงคาดว่าทำงานได้เหมือนกัน **แต่ขอให้ฝ่าย IT
  ทำการทดสอบซ้ำตามหัวข้อ 8 บน Edge จริงอีกครั้งก่อนเปิดใช้งานจริง** เพื่อความมั่นใจ

---

## 10. Known Limitation

- **เวอร์ชันปัจจุบันเก็บข้อมูลแบบ browser-local persistence (`localStorage`) เท่านั้น
  ยังไม่มี central database หรือ multi-user synchronization**
- หมายความว่า:
  - ข้อมูลที่นำเข้า และผลการพิจารณา (เห็นชอบ/ทบทวน/ไม่เห็นชอบ + หมายเหตุ) จะถูกเก็บอยู่ใน
    เบราว์เซอร์ของเครื่อง/ผู้ใช้คนนั้นๆ เท่านั้น
  - หากผู้บริหารหลายคนต้องพิจารณาร่วมกัน หรือพิจารณาจากหลายเครื่อง **จะไม่เห็นผลการพิจารณาของกันและกัน
    แบบเรียลไทม์** — ต้องใช้วิธี **Export ผลเป็น CSV** แล้วส่งต่อ/รวมกันเอง หรือให้ทุกคนนำเข้าไฟล์
    Excel ต้นทางเดียวกันตามลำดับ
  - หากล้างข้อมูลเบราว์เซอร์ (Clear browsing data / เปลี่ยนเครื่อง/เปลี่ยนเบราว์เซอร์) ผลการพิจารณาที่
    ยังไม่ได้ export ออกมาจะหายไป — ควร export CSV เก็บไว้เป็นระยะ
  - นี่คือข้อจำกัดที่ทราบและตั้งใจออกแบบไว้เช่นนี้ในเวอร์ชันปัจจุบัน (ไม่ใช่บั๊ก) — หากในอนาคตต้องการให้
    ผู้บริหารหลายคน/หลายเครื่องเห็นข้อมูลเดียวกันแบบ real-time จะต้องพัฒนาเพิ่มเติมส่วน backend/database
    ซึ่งอยู่นอกขอบเขตของเวอร์ชันนี้
