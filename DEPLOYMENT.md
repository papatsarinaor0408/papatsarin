# คู่มือ Deploy

**ระบบพิจารณาแผนพัฒนาบุคลากร (โรงไฟฟ้าบางปะกง) ปี 2570**

> **อัปเดตสำคัญ:** เวอร์ชันนี้เพิ่มระบบ Login/สิทธิ์การใช้งาน (Admin/Reviewer) และฐานข้อมูลกลางออนไลน์
> (Supabase) แล้ว — **ต่างจากเวอร์ชันก่อนหน้าที่เป็น static ล้วนและไม่ต้องมีอินเทอร์เน็ต** เอกสารนี้เขียนใหม่
> ทั้งหมดให้ตรงกับสถาปัตยกรรมปัจจุบัน

---

## 1. ประเภทของระบบ

ระบบนี้ประกอบด้วย 2 ส่วน:

- **Front-end**: ไฟล์ HTML/CSS/JavaScript แบบ static (ไม่มี build step) — โฮสต์อยู่บน **GitHub Pages**
- **Back-end**: **Supabase** (Postgres + Auth + Row Level Security) — ให้บริการฐานข้อมูลกลาง, ระบบ Login,
  และ API สำหรับบันทึกผลการพิจารณา ทั้งหมดผ่าน HTTPS โดยตรงจากเบราว์เซอร์ผู้ใช้ (ไม่มี server-side code
  ของเราเองที่ต้องดูแล นอกจาก Supabase Edge Functions 2 ตัวสำหรับงานเฉพาะที่ต้องใช้สิทธิ์สูง — ดูหัวข้อ 4)

**Entry point:** `login.html` (หน้า Login) → หลัง Login สำเร็จจะเข้าสู่ `index.html` (แดชบอร์ดหลัก)

**Live URL:** `https://papatsarinaor0408.github.io/papatsarin/login.html` (deploy จาก branch
`claude/personnel-development-plan-approval-ttihq8` โดยตรง ผ่าน GitHub Pages settings — ไม่ได้ merge เข้า `main`)

---

## 2. รายการไฟล์/โฟลเดอร์ที่ต้อง Deploy (front-end)

```
index.html                          ← แดชบอร์ดหลัก (ต้อง login ก่อนถึงจะเข้าได้)
login.html                          ← หน้า Login + บังคับตั้งรหัสผ่านใหม่ครั้งแรก
assets/
├── css/style.css                    ← สไตล์/ธีม (รวม auth page)
└── js/
    ├── schema.js                    ← นิยามคอลัมน์ Excel และสถานะการพิจารณา
    ├── importer.js                  ← ตัวอ่านไฟล์ Excel/CSV (client-side)
    ├── charts.js                    ← เครื่องมือวาดกราฟ SVG
    ├── supabaseClient.js            ← การตั้งค่า Supabase client (URL + publishable key — ปลอดภัยที่จะ public)
    ├── auth.js                      ← Login/Logout/session gating
    ├── permissions.js               ← ซ่อน/แสดงปุ่มตาม role (UI เท่านั้น ไม่ใช่ตัวคุมสิทธิ์จริง)
    ├── dataClient.js                ← เชื่อมต่อฐานข้อมูลกลางผ่าน Supabase (แทน localStorage เดิม)
    ├── app.js                       ← ตรรกะหลักของแอป
    └── vendor/
        ├── xlsx.full.min.js         ← SheetJS (อ่านไฟล์ Excel) — แนบมาในเครื่อง ไม่โหลดจาก CDN
        └── supabase-js.min.js       ← Supabase JS client — แนบมาในเครื่องเช่นกัน
```

**ไม่มี** ไฟล์ข้อมูลจริง/ข้อมูลส่วนบุคคลของพนักงานอยู่ใน repository นี้เลย — ไฟล์ Excel ที่ใช้สร้างบัญชี
ผู้ใช้และไฟล์แผนงานจริง อยู่นอก repository เสมอ (ดูหัวข้อ 6)

---

## 3. Supabase (back-end) — ตั้งค่าไว้แล้ว

- Project: `pdp2570-egat-bangpakong` (region Singapore) — แผน **Free Tier**
- Schema (`profiles`, `plans`, `decisions`, `audit_logs`) พร้อม Row Level Security เปิดใช้งานครบทุกตาราง —
  ดู SQL จริงที่ `supabase/migrations/` ใน repository นี้
- Edge Functions 2 ตัว (ดูหัวข้อ 4)

**Publishable key ที่ฝังใน `assets/js/supabaseClient.js` ปลอดภัยที่จะเปิดเผยต่อสาธารณะโดยเจตนา** —
Supabase ออกแบบมาให้ค่านี้ใช้ในเบราว์เซอร์ได้ ตัวที่คุมสิทธิ์จริงคือ Row Level Security ฝั่งฐานข้อมูล
ไม่ใช่ความลับของ key นี้ — **ห้ามสับสนกับ `service_role`/secret key ซึ่งต้องไม่ปรากฏที่ front-end เด็ดขาด**

---

## 4. Edge Functions (งานที่ต้องใช้สิทธิ์สูงสุด — ทำงานฝั่ง Supabase server เท่านั้น)

| ฟังก์ชัน | หน้าที่ | หมายเหตุ |
|---|---|---|
| `seed-accounts` | สร้างบัญชีผู้ใช้ครั้งแรก (Admin 1 คน + Reviewer 395 คน) จากไฟล์ Excel รายชื่อพนักงาน | ใช้ครั้งเดียวตอนติดตั้งระบบ ป้องกันด้วย secret แยกต่างหาก (`SEED_SECRET`) เมื่อสร้างบัญชีครบแล้วจะถูกปิดการใช้งาน |
| `reset-password` | Admin (เลขประจำตัว 596203 เท่านั้น) รีเซ็ตรหัสผ่านให้ผู้ใช้คนอื่น | ตรวจสิทธิ์ผู้เรียกฝั่ง server ทุกครั้ง ไม่เคยบันทึกรหัสผ่านลง log |

ทั้งสองฟังก์ชันใช้ `service_role` key ที่ Supabase ฉีดให้อัตโนมัติในสภาพแวดล้อมของ Edge Function เอง
**ไม่เคยปรากฏใน front-end, ไม่เคยอยู่ใน repository, ไม่เคยอยู่ในแชทหรือ log ใดๆ**

---

## 5. Browser Requirements

รองรับเบราว์เซอร์ evergreen รุ่นใหม่ทั่วไป: **Google Chrome**, **Microsoft Edge** (Chromium-based),
Firefox รุ่นใหม่ — **ไม่รองรับ Internet Explorer** โค้ดใช้ ES6+ มาตรฐาน, `fetch`/HTTPS ไปยัง Supabase,
`localStorage` เฉพาะสำหรับ session token ของ Supabase และการตั้งค่าธีม (light/dark) เท่านั้น
**ไม่ใช้ localStorage เก็บข้อมูลแผน/ผลการพิจารณาอีกต่อไป**

---

## 6. Security — สรุปสำคัญ

- **ไม่มี password/วันเกิด/ไฟล์ Excel รายชื่อพนักงานอยู่ใน repository นี้เลย** — ใช้เฉพาะตอน seed บัญชีครั้งแรก
  จากเครื่อง local ของผู้ดูแลระบบเท่านั้น ไม่เคย commit เข้า git
- Password ทุกบัญชีถูก hash โดย Supabase Auth (ไม่มีการเก็บ plain text ที่ใดเลย)
- Login ครั้งแรกบังคับให้ตั้งรหัสผ่านใหม่ก่อนเข้าใช้งาน (Initial Password จากวันเกิดใช้ได้ครั้งเดียว)
- สิทธิ์ Admin ผูกกับเลขประจำตัว `596203` เท่านั้น ตรวจสอบซ้ำที่ฐานข้อมูล (RLS + ฟังก์ชัน `is_admin()`)
  ทุกครั้งที่มีการเรียกใช้งาน ไม่ใช่แค่ซ่อนปุ่มที่หน้าเว็บ
- ทุกการนำเข้า/แก้ไข/ลบข้อมูล และการพิจารณาแผน ถูกบันทึกลง audit log พร้อมเวลาเซิร์ฟเวอร์ (ไม่ใช้เวลาเบราว์เซอร์)

---

## 7. Network Dependency

ต่างจากเวอร์ชันก่อนหน้า **เวอร์ชันนี้ต้องมีการเชื่อมต่ออินเทอร์เน็ตตลอดเวลาที่ใช้งาน** เนื่องจากต้องติดต่อ
Supabase (`https://tmehsrxvninpatqxhcgs.supabase.co`) สำหรับ Login และข้อมูลทุกครั้ง — **ไม่สามารถใช้งาน
แบบ offline ได้อีกต่อไป** (จุดนี้เป็นการเปลี่ยนแปลงที่จำเป็นจากการทำระบบ Login + ฐานข้อมูลกลางหลายผู้ใช้)

ฟอนต์ Kanit ยังคงโหลดจาก Google Fonts เช่นเดิม — ถ้าโหลดไม่ได้จะ fallback ไปฟอนต์ระบบ ไม่กระทบการทำงาน

---

## 8. วิธีทดสอบหลัง Deploy

1. เปิด URL หน้า Login → ต้องเห็นฟอร์ม เลขประจำตัว/รหัสผ่าน ไม่ใช่แดชบอร์ดทันที
2. Login ด้วยบัญชีที่ยังไม่เคยเปลี่ยนรหัสผ่าน → ต้องถูกบังคับไปหน้าตั้งรหัสผ่านใหม่ก่อน
3. ตั้งรหัสผ่านใหม่สำเร็จ → ต้องเข้าสู่แดชบอร์ดได้
4. Header มุมขวาบนต้องแสดงตำแหน่งของผู้ใช้ที่ login และปุ่ม "ออกจากระบบ"
5. บัญชี Reviewer ต้อง**ไม่เห็น**ปุ่ม "นำเข้าข้อมูล"/"ล้างผลพิจารณา" (Admin เท่านั้น) และเปิดดูรายละเอียด
   แผนแล้วต้อง**ไม่เห็น**ปุ่มเห็นชอบ/ทบทวน/ไม่เห็นชอบเลย (เห็นได้แค่ผลที่ Admin บันทึกไว้แล้ว ดูอย่างเดียว)
6. Login ด้วยบัญชี Admin (596203) พิจารณาแผน 1 รายการ (เห็นชอบ/ทบทวน/ไม่เห็นชอบ) → รีเฟรชหน้าเว็บ →
   ผลต้องยังอยู่ (มาจากฐานข้อมูลกลาง)
7. Console (`F12`) ต้องไม่มี JavaScript error สีแดง
8. ทดสอบบนทั้ง Chrome และ Edge

---

## 9. Known Limitation

- Free Tier ของ Supabase: โปรเจกต์อาจ pause อัตโนมัติหากไม่มีการใช้งานติดต่อกัน ~7 วัน (ต้องกด resume ที่
  Supabase Dashboard) — ไม่กระทบข้อมูล เพียงแต่ต้อง resume ก่อนใช้งานรอบถัดไป
- ระบบต้องพึ่งพาอินเทอร์เน็ตเชื่อมต่อ Supabase ตลอดเวลา (ดูหัวข้อ 7)
- ยังไม่มีหน้าจอจัดการบัญชีผู้ใช้ (เปิด/ปิดบัญชี, รีเซ็ตรหัสผ่าน) แบบ UI — ฟังก์ชันหลังบ้านพร้อมใช้งานแล้ว
  (เรียกผ่าน API ได้) แต่ยังไม่มีหน้าจอสำหรับ Admin กดใช้งานเอง เป็นการพัฒนาต่อในอนาคต
